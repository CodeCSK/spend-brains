# Template for apps/web/.env.staging (Fly API + beta login hints)
# npm run dev:staging

VITE_API_URL=https://spendbrains-api-beta.fly.dev
# Show beta OTP hint on login (shared code — private friends only)
VITE_BETA_MODE=true
