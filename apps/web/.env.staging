# Vite staging mode — loaded automatically by npm run dev:staging
# Public beta API URL (same as Cloudflare Pages production build)
VITE_API_URL=https://spendbrains-api-beta.fly.dev
# Show beta OTP hint on login (last 6 digits of phone)
VITE_BETA_MODE=true
